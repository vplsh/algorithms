#pragma once

#include <iostream>

using namespace std;

class MathAlgorithms {

public:

	double Min(double Array[], const int Size);
	double Max(double Array[], const int Size);
	double Sum(double Array[], const int Size);
	double Average(double Array[], const int Size);
};